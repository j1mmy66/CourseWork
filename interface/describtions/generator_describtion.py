GENERATOR_INFO_MD = """
**Синтетический генератор данных**  
Создаёт *K* гауссовых кластеров в *V*‑мерном пространстве. Каждый кластер
получает минимум `n_min` точек, а оставшиеся распределяются случайно.
Параметр `alpha` контролирует взаимное перекрытие центров:  
α→0— кластеры разнесены, α→1— центры ближе и кластеры сильнее «путаются».

**Параметры**

- **`N`** — общее количество объектов в выборке.  
- **`V`** — количество признаков (размерность пространства).  
- **`K_star`** — требуемое число кластеров (*K*★).  
- **`n_min`** — минимальное число объектов в каждом кластере  
  (должно выполняться `N ≥ K_star×n_min`).  
- **`alpha`** — степень перекрытия кластеров, 0<α<1  
  (меньше → кластеры дальше, больше → ближе друг к другу).
"""
